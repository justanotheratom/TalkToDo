import SwiftUI
import TalkToDoFeature

struct ContentView: View {
    var body: some View {
        MainContentView()
            .frame(minWidth: 400, minHeight: 300)
    }
}

#Preview {
    ContentView()
}
