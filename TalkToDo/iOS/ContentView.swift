import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Text("TalkToDo")
                .font(.largeTitle)
                .padding()

            Text("Phase 1: Project Setup Complete")
                .foregroundStyle(.secondary)
        }
    }
}

#Preview {
    ContentView()
}
